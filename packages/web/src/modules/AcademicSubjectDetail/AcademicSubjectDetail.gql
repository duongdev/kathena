query AcademicSubjectDetail($id: ID!) {
  academicSubject(id: $id) {
    id
    code
    name
    description
    imageFileId
    publication
  }
}
mutation UpdateFile($id: ID!,$newFile: Upload!){
  updateFile(id: $id, newFile: $newFile) {
    id
    name
  }
}
