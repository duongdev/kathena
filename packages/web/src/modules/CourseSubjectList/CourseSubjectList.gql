query Courses($orgId: ID!, $skip: Int!, $limit: Int!) {
  courses(
    pageOptions: { skip: $skip, limit: $limit }
    filter: { orgId: $orgId }
  ) {
    courses {
      id
      code
      createdAt
      name
      orgId
      academicSubjectId
      startDate
      tuitionFee
      lecturerIds
    }
    count
  }
}
